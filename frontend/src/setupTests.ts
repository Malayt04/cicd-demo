import "@testing-library/jest-dom";

// Add any global test setup here
beforeAll(() => {
  // Setup any global test environment if needed
});

afterAll(() => {
  // Cleanup any global test environment if needed
});
